# git仓库搭建以及管理
## 搭建git仓库的步骤
1. 安装git
2. 配置git
3. 创建git仓库
4. git常用命令
   ```
   git init                      # 初始化一个新的git仓库
   git add .                     # 添加所有文件到暂存区
   git commit -m "initial commit" # 提交暂存区的文件到仓库
   git branch -M main            # 重命名默认分支为main
   ```
## 上传git仓库至github的步骤
1. 注册github账号
2. 在github上创建一个新的仓库
3. 初始化本地仓库
4. 添加远程仓库
5. 推送本地仓库至远程仓库